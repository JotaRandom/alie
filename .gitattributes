# Git attributes for ALIE project
# Ensures proper line endings for cross-platform compatibility

# Shell scripts must always use LF line endings (Unix-style)
# This is critical for Linux/Unix execution environments
*.sh text eol=lf

# Markdown files can use system defaults but prefer LF for consistency
*.md text eol=lf

# License files should use LF for cross-platform compatibility
LICENSE text eol=lf

# Configuration files in configs/ directory
# All config files must use LF for Linux compatibility
configs/**/*.conf text eol=lf
configs/**/*.template text eol=lf
configs/**/nanorc text eol=lf
configs/**/vimrc text eol=lf
configs/**/bashrc text eol=lf
configs/**/zshrc text eol=lf
configs/**/config.fish text eol=lf
configs/**/*.sh text eol=lf

# Git configuration files
.gitignore text eol=lf
.gitattributes text eol=lf

# Ensure text files are detected and handled properly
* text=auto

# Binary files (none currently, but good to have)
# *.png binary
# *.jpg binary
# *.gif binary